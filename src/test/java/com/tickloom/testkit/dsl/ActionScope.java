package com.tickloom.testkit.dsl;

/**
 * Marker interface for protocol-specific action verbs exposed inside a step
 * (e.g. {@code QuorumActionScope} with {@code writes(...)} / {@code reads(...)}).
 */
public interface ActionScope { }
